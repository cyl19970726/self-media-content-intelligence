# 分析工作台稳定化记录

状态：本轮阅读与兼容稳定基线已完成。范围为单帖与单博主，不融合生产发布流程。

## 基线

沿用已完成的单帖、单博主闭环记录及认知 V0，增加此前未深读的内容类型复验。2026-09-10 已确认当前 5181 页面服务可访问。独立读者使用本目标唯一浏览器空间 33；参与过上一轮，因此本次属于复验，不称全新上下文盲测。原始记录保存在 `.runtime/stabilization-20260910/`。

## 问题与状态

| 问题 | 依据 | 处理与验证 |
| --- | --- | --- |
| 图片引用无法解析时，Builder 的视觉注释被连同媒体一起丢弃；步骤图片引用也会消失 | `src/server/video-research.ts` 仅在 frameLookup 命中时保留 visuals 和步骤媒体；构造缺失引用后复现字段丢失 | 增量保留 unresolvedVisuals 和 unresolvedFrameRefs；页面显示原说明、支持范围、证明边界和未解析引用；不补写结论。投影和渲染共 12 项测试通过；全仓串行 80 文件、406 项通过、1 项条件跳过；构建、类型、受影响文件 lint、repo 和 docs 检查通过。尚待页面复验 |
| 历史 API 教程正文引用 KU，读者无法就近到达其画面；编导/视听入口未提供对应结构字段 | 读者实际阅读 API 教程 67af4c9f，审计中可找到 KU32–34 的画面、声音和剪辑说明 | 待核对原始 KU→证据关联。不得把知识单元拼写为 Builder Lens 结论；判断是否应增加引用解析，而非重分析 |

## 架构初查

当前路径是原始 reconstruction → `src/server/video-research.ts` 投影 → `videoResearchSchema` → 专门报告组件。职责分层可以保留，但需要检验投影是否保存缺失媒体附近的语义、局部字段是否受整体完整性条件影响，以及未知字段是否仍可从原始资产到达。上述问题尚未全部完成核查。

主导航目前同时展示总览、链接分析、博主研究、多博主、迭代验证、内容知识、证据存储、创作发布和 Notion NEXT 占位。此记录只确认存在，不据此认定无用或直接删除；功能取舍将在单博主阶段结合实际任务和依赖形成清单。

## 单帖追加闭环

独立读者实际读完秋芝 API 教程、AI《单纯》歌曲、张 Tab Out 产品故事、身份标签播客，以及开发环境新三 Lens 报告，共五份；前四份此前未深读。主库 5181 与开发样本 5177 分开记录，原帖时间、表现及评估身份不混用。

API 的 KU-15 在接口中确有 TARGET-0113 等引用。正文现可展开明确引用的 KU 原文、未知与既有证据，不添加未引用的知识单元，不改写为 Lens。读者实点成功打开 835.5 秒 360px 画面；OCR 未解析仍如实提示。此次页面阻断已闭合，单博主复验随之启动。

兼容异常分支另复现：将画面 carriers 置空，会导致不相关的编导 label/viewerQuestion 和 visual notes 丢失。已取消这些展示字段对整套 Lens 完整性的依赖，保留已有值；不改变评估结论。回归测试先失败再通过。

源研究边界单列：播客原记录同时声明转写缺口与“完整”措辞；新 Lens 成果段引用含晚于段落的60秒帧；Tab Out阶段边界句未必直接支持阶段核心。这些不能通过改写原文解决，也不能把页面可读当成源分析质量已通过。后续需要逐项回源核验，当前明确保留为未验证的研究问题。

## 单博主复验与修复

秋芝综合反例 → Qclaw 单帖 → 原研究批次高中低章节；张基本盘 7 条筛选 → Tab Out → 保留 tier=base 返回，两条路径均由独立读者实际完成。读者能解释秋芝 70/21/12 的不同分母和高中低表现层、中位/均值选样角色，未把局部样本外推为全部作品。

阶段卡的两个问题已闭合：采集重试策略被误当作仍存在媒体缺口，导致 12/12 仍提示补取；重建任务完成被文案扩大成当前三 Lens 字段齐全。修复后，仅实际缺失数量大于零才应用不可得分支；重建卡说明任务产物和原验证记录，不替代字段及当前评估。独立页面刷新复验通过，pipeline 行为测试覆盖旧缺口仍在和缺口已恢复两种状态。

## 功能取舍与架构结论

| 功能 | 决定及依据 |
| --- | --- |
| 单帖、单博主、目录、筛选与证据往返 | 保留主路径。实际复述和核验任务使用，不能合并成摘要替代原报告 |
| 工作总览 | 保留，用于发现已有资产与进入研究；本轮未扩展创作资产统计 |
| 多博主、知识、创作发布 | 保留现有入口和数据。已有真实资产与下游依赖，本轮不扩展；未使用不是可删除依据 |
| 迭代验证、证据存储 | 保留用于审计和恢复，不加入主报告正文 |
| 博主页知识贡献 | 收进可展开研究审计。读者确认空卡对主阅读无收益，但已有记录仍需可达 |
| Notion NEXT | 删除无操作占位。无对应页面行为，移除不影响资产与流程 |

架构采用现有专用组件和契约投影即可，本轮证据不支持大规模重写。已检查 schema 的 contentRestoration、directingLogic、visualEditing 顶层字段：内容在连续报告，包装及开头在明确的阅读入口，编导与视听其余字段在各自组件。适配层处理原字段、媒体引用及版本身份，不能推导新的内容结论；此次两项数据丢失修复说明“局部缺失不得控制其他字段”的约束需要持续保留。

后续新增字段必须同时明确 schema、投影、呈现位置和缺失兼容测试；当前未知字段仍可在原始 reconstruction 资产追溯，不能因此声称未来任意新 schema 都会自动获得专门排版。没有必要提前建设通用页面引擎。

知识贡献请求另加入对象切换后的失效保护，避免旧请求晚返回覆盖新博主记录。这是读取状态隔离，不改变知识裁决或生成新知识。

## 稳定基线的边界

本轮验证的是阅读、引用、字段保留、批次/状态语义和功能路径。不是所有原分析结论已被重新验证，也不是所有视频已逐帧重看。源冲突及效果因果问题继续保持未验证；不重跑模型、不写原库、不融合生产发布。

## 最终验收

- 单帖五份新增/回归阅读及此前高中低基线有界复用；KU 引用修复实点通过。
- 两位博主的综合、分母、筛选、引用和原批次返回通过；第08/09阶段卡刷新复验通过。
- 最后390px复验确认主报告与导航可读、贡献折叠可展开、Notion占位已移除；截图与独立记录保留于本地 `.runtime/stabilization-20260910/`。space33已成功关闭。
- 80个测试文件通过，407项通过、1项条件跳过；完整构建、全仓lint、类型、repo与docs检查通过。采用已记录的串行测试策略；未声称既有并行快照问题已解决。
- 修复均为投影、显示及状态修正，原始报告、证据与业务数据未改写。未开展生产融合。

## 后续异常分支复查

用户要求继续检查后，追加三项可复现修复：

- 操作序列的 `frameRefs` 若包含未重复登记在 steps 或 evidenceRefs 的画面，投影会将其丢弃。现在仅去掉已在步骤显示的重复兜底媒体，其余段落画面仍保留；Builder 专门视觉标注不去重删除。
- 无起始时间的转写证据原显示 `undefineds`，改为“时间未知”，不猜时间。
- 未提供结构化 openingAnalysis 时，不再断言旧报告“需重新分析”；明确已有内容可从原文阅读，是否补分析取决于实际信息缺口。

以上通过构造数据先复现再修复，本次未新增真人或浏览器验收。先前记录的源分析冲突未被这些显示修复解决。
