# Issue 71：小型可组合 workflow 实施记录

## 目标

在独立 `packages/workflow` 提供最小可组合运行能力，Codex SDK和现有持久化通过adapters接入；research维护单帖和博主流程，工作台展示同一执行记录及中间资产。按节点恢复而非任意语句恢复，保留事实源及独立复核边界。

## 交付与状态

- [x] 核对现有调度/资产/前端接点，固定SDK实际类型和版本（0.154.0）。
- [x] workflow package、状态/资产/事件合同与恢复/取消测试。
- [x] Codex SDK适配器与私有trace，SQLite存储接入。
- [x] 单帖与博主业务流程及现有服务桥接，避免竞争调度（真实完整博主父流程验收另列）。
- [x] 工作台执行快照、资产索引、trace摘要、重试/取消入口；已实际阅读单帖与综合候选。
- [ ] 类型/边界/相关回归测试；真实失败样本和内建浏览器验收。

## 执行约束

现有批量进程不在迁移中中断。Terra medium Builder / Luna medium Reviewer显式配置。不得用格式修复伪造证据或放宽门槛。私有trace不发布到公共静态目录。保留已有未提交改动。正式实现细节以测试和运行证据为准，未验证能力不得标完成。

## 与 issue #71 的实现映射（2026-09-16）

| Issue 要求 | 实现位置 / 证据 | 当前状态 |
| --- | --- | --- |
| 独立、小型代码编排 package | `packages/workflow`；不依赖业务或应用 | 已实现 |
| SDK 调用及实际模型/Skill/输入 trace | adapters/workflow/codex-sdk-runner；旧完整执行器显式 SDK 模式 | 已完成单帖 Luna 修复、综合 Terra 构建与 Luna 复核实跑 |
| 持久节点、attempt、资产、事件 | adapters/workflow/sqlite-run-store | 已通过持久化与增量事件测试 |
| 失败评估局部恢复 | validated Builder node replay + post reviewer-only | 已完成真实原始 relation 失败评估定向修复，正文 SHA 不变 |
| 父子等待、进程恢复、取消传播 | ChildWorkflowDispatcher + research-workflow-executor | 已通过重建 executor / 取消测试 |
| 唯一队列与真正并发 | research_jobs workflow.advance；v2 父流程 control/synthesis lane、post 子执行 video lane cap=2，v1 保留原 lane | 已通过并发 claim 与旧任务隔离测试 |
| 完整博主流程 | creator.analyze：post children 后冻结新的 synthesis 输入 | 已实现，真实验收待完成 |
| 工作台进度、中间资产、恢复操作 | workflow-runs 页面/API + creator 入口 | 实际浏览器已读父子进度、完整单帖三 Lens 与综合候选 |
| 新旧同输入对照与最终阅读 | 下述真实执行记录 | 未完成，不可关闭 issue |

## 真实执行发现与修复记录

### 初次审计缺口（下方记录后续修复）

已重新获取 GitHub issue 正文（updatedAt `2026-09-15T16:33:24Z`），与本次实施依据一致。以下缺口不能以“框架已有”替代验收：

- v1 build/review/repair 是独立持久节点，但尚非可独立调用的小 workflow；v2 正在拆分，保留旧定义恢复。
- v1 可以仅重新运行评估，但还没有针对错误字段和旧评估文件的独立定向修复路径。
- workflow 资产已落盘，但尚未通过幂等注册器更新原博主详情/列表的正式版本指针。
- 通用 JSON 树不能作为三 Lens 阅读器。实际浏览发现深层字段截断、证据未就近显示，正在接回既有阅读器与精确候选投影。
- 全仓测试最新 481 passed / 1 skipped；这不代表真实博主闭环已验收。

### 成功的候选复用与独立重评（不等于定向字段修复）

`9a523b25-80c7-4f67-84f2-41d70f56c39d` 已完成。复用原报告，正文 SHA-256 保持 `bdbbc0c71826236abbdb06eca915a79bc4173c48a2d954c7648c21410111533c`；实际只有一次模型执行，Luna medium（Codex SDK/CLI 0.154.0），没有重跑 Builder。评估通过合同校验并登记 `post-evaluation`，保留独立候选与评估依赖。

SDK报告用量：input 890735，cached input 809088，output 14748，reasoning output 1474；这是调用累计 token 字段，不等于新增上下文大小或费用。运行摘要存于本地 `.runtime/issue-71/post-success-evidence.json`，原始 trace 私有。仍需 v2 的真实局部修复与博主综合验收。

- 使用铁卫士真实失败样本 `69e481e8000000002003ad94`，来源 run `92203c70-31bf-4240-8c25-7a1743117462`。
- 新 workflow `402af275-82dc-4fc5-859d-18b314ee6219` 复制既有候选后，媒体准备 manifest 仍指向旧目录，导致本地路径投影校验报 `BUILDER_INTEGRITY_EVIDENCE_FINGERPRINT`，触发了不应发生的完整性修复。发现后立即通过取消 API 停止，执行进程返回 canceled；旧报告未被改写。
- 该次试跑不是成功验收。保留 trace 和状态作为迁移回归证据，修复候选复用路径后需重新验证。后续定位确认是 `media-preparation.json.evidencePack.path` 的旧绝对路径，而非证据内容发生变化；修复仅重定位本地路径，冻结正文与证据哈希不变。
- 内建浏览器发现纯技术事件名不足以说明进度；已改为中文阶段/生命周期摘要，并将计划模型与实际调用信息、未知用量分开呈现。


## V2 集成验收更新（2026-09-16）

已落地独立的 `post.build/review/repair/repair-evaluation` 和 `creator.build/review/repair` 子流程；父流程等待时释放队列租约。加入版本注册器，将经校验的候选和复核结果以来源版本检查、事务与幂等方式接回原工作台。原有 workflow v1 定义保留用于恢复，不是旧报告格式兼容层。

497 项全仓测试通过、1 项跳过；构建、lint、目录边界和编译后 Web/API/Worker/CLI 启动检查通过。这是修复下列最新实跑缺口之前的检查结果，后续改动需要重新执行相关检查。

真实 V2 综合 `e762a5ba-d492-4cfe-ab10-d88c1d1d6e7b` 复用 AI硬件情报局已登记的 12 篇单帖输入，不声称本次重建了这些单帖。初次执行发现新子流程 ID 未加入现有队列 lane，`creator.build` 无法领取；已补充领取、并发上限与父流程释放槽位回归，随后从原运行继续，没有新建重复根任务。子流程 `93474595-5a11-4d95-bbb1-6bed318d27fa` 已进入真实 Terra medium SDK 执行；尚未完成综合闭环。

内建浏览器实际确认父流程显示“等待子流程”，并可进入正在执行的子流程。实际阅读还发现只有综合运行而无新单帖 workflow 时，原博主页进度区域被条件隐藏；正在修复。已完成的单帖候选阅读器实际展示连续内容还原、编导及画面剪辑，并就近显示帧证据，未改写 Builder 原文。

剩余验收必须分别记录，不能用单测数量替代：

- 原始无效评估的显式导入和定向修复，候选正文哈希不变。
- V2 综合构建、独立复核、正式版本登记及页面阅读。
- 完整 `creator.analyze` 父流程的真实多单帖子任务运行；仅运行综合不等于验收此项。
- 同冻结输入的新旧路径成本与耗时对照；当前只有局部执行证据，不能据此声称整体节省。


### 真实综合候选阅读

已在内建浏览器实际打开 `creator.build` 的版本化候选。读到：账号常以商业数字或反常识承诺开场，将产品差异与实现约束并置；TuyaClaw 的连续配置步骤与 OpenClaw 的概念素材并非同等证明；高、中、低公开点赞层都有商业/趋势叙事，不能据此作效果归因。候选列明 196 条可见作品、21 条比较样本、12 条深读候选及后台指标未知。

阅读发现版本化文件注册位置与阅读器前缀假设不一致，导致候选 404；已支持受同 run、SHA 与冻结来源约束的 `creator-analysis.<hash>.json`，加入回归，实际浏览成功。候选视图缺少全量基本盘区域，因此移除其中的无目标锚点链接；正式博主页仍保留该入口。

独立 Reviewer 已完成，但综合父流程因适配器将允许交付的 `provisional` 错映射为 `source_gap` 而阻塞。底层 outcome 为 provisional，唯一未过的正式晋升 gate 为 `deep_9_ready`，不应把它改成 verified，也不应丢失带边界可读的交付资格。正在修复映射，保留这次真实失败记录。

### 落盘与完整性缺口

第一次 V2 定向修复尚未调用模型就因可选 undefined 字段被 JSON 省略而触发恢复指纹不一致。已统一对持久化 JSON 值计算指纹与资产哈希，并验证资产每次读取的 SHA。原失败 `2aa7364f-b2e8-4416-a62a-fe3f039eedd7` 保留；新运行 `04894323-a42b-4da9-ab44-a67ab8e83016` 已复用候选并识别原评估合同错误，进入独立 `post.repair-evaluation`。


### 真实定向评估修复已完成

V2 `04894323-a42b-4da9-ab44-a67ab8e83016` 已 succeeded：`post.build` 显式复用候选，`post.review` 导入原始无效评估返回 needs_review，`post.repair-evaluation` 通过一次独立 Luna medium 调用完成修复，随后 register-version 登记到 `video-reconstruction-batch-r13.04b981c70a93.json`，该帖真实状态为 verified。原始失败评估与子流程记录保持可追溯；父流程成功没有抹去原错误。

候选原始与当前正文 SHA 均为 `bdbbc0c71826236abbdb06eca915a79bc4173c48a2d954c7648c21410111533c`。本次没有 Builder 模型调用。Luna SDK/CLI 均为 0.154.0；实际累计 input 1,093,018、cached input 993,152、output 14,093、reasoning output 2,689。相比前面的同候选整份重评，输入用量没有下降；定向任务保住了 Builder 资产，但现有评估操作范围仍较大，不能宣称 token 优化已完成。证据摘要存于 `.runtime/issue-71/post-repair-v2-success-evidence.json`。

最新全仓回归 510 passed / 1 skipped；构建、lint、目录规则、文档检查和编译后入口 smoke 全部通过。此后任何代码改动需补相应检查。


### Host 对同输入综合的阅读判定

当前可读综合 `creator-analysis.a016cab7b718.json` 与新候选 `creator-analysis.4019cc5ad224.json` 绑定的五份输入完全一致。旧版五章含 9 个 finding，新候选含 5 个；数量仅作定位线索，不是质量标准。实际阅读的差异是：旧版分别讲清拉链结构、系统状态远控、灵巧手数据链的不同问题，商业链也区分摄像头订阅、餐饮复制与平台入口；新候选把更多内容压为“差异—约束—价值”的概括。它有可读价值，但还不能证明保住了既有研究深度。

因此不为满足登记验收而替换现有更丰富版本。修复 provisional 路由之后，可针对已完成且同版绑定的产物做独立、可追溯的登记恢复；本轮只准备并校验该恢复，不执行晋升。原综合父流程的 blocked 实跑记录保留。完整博主父流程、综合读者质量和新旧成本对照仍是未完成验收项；不能关闭 issue #71。


收束检查：全仓 512 passed / 1 skipped；最终 trace 版本记录修改的 6 项定向测试及 TypeScript 检查通过；构建与编译后的 Web/API/Worker/CLI smoke 通过。项目内 reader-led-workflow 的运行引用已同步 SDK/旧 CLI 边界，skill 校验通过；没有全局安装或改写全局 skill。综合登记恢复脚本只完成只读预检，不替换当前报告。后续 trace 增补 SDK/CLI 顶层版本字段会被方法快照感知，不能回填成旧运行当时已经记录的字段。

### Skill 交付核验与完整父流程首跑

每次 Agent 启动前读取其明确需要的方法文件，拒绝缺失/空文件，将正文快照加入实际 prompt。私有 `skillLoad` receipt 记录文件路径、SHA、字节数、prompt 锚点以及整体 effectivePromptSha256。单帖 Builder/Evaluator 使用各自 operator 合同；不将通用 SKILL.md 强塞给明确禁止读取它的角色。博主综合使用项目内 SKILL.md 和 method.md。此机制证明方法已交付，不证明模型遵守方法；后者仍由校验、独立评估和读者验收负责，也不冒称 Codex 原生 skill 自动发现。

`cwd` 仍为本次执行的独立产物目录，与 skill 来源目录分离。通用 SDK adapter 已支持逐 Agent 配置 `config.skills`，现有生产研究角色的方法清单由适配器声明，尚未提供工作台可视化 skill 选择。外层 TypeScript workflow 管理循环、分支、队列和资产，SDK 执行 Agent，skill 提供方法，不承担持久调度。

核验新增后 43 项定向测试与 TypeScript 检查通过；补全评估修复实现和 overview 脚本的方法冻结范围后，研究输入测试再次通过。真实父流程 `b19b8cd1-73bd-40d9-954b-c698945b7757` 已针对 AI硬件情报局的 12 篇准备好媒体的样本启动，使用既有队列、两个单帖执行槽和一个控制/综合 worker。启动显式指定 Codex 可执行文件，避免 PATH 版本与 SDK 默认内置二进制混淆。复用既有候选需重新通过完整性检查；首批已有两篇被 core_evidence_count 阻断，不能把父流程已启动说成完整研究已通过。运行 receipt/log 位于 `.runtime/issue-71/creator-parent-*`。

### Phase 集成执行计划（2026-09-16）

当前目标：按 #71 Phase 补充要求实现最小可用闭环。阶段合同和持久归属、关键资产引用、安全 API 投影、行式阅读入口、新版本研究流程接入、控制调度修复分别落地。保留正在运行的 v2，不伪造历史 Phase；新增 v3 才声明阶段。

验收：并行/嵌套/暂停恢复归属测试；真实类型资产通过原阅读器可读；阶段的执行与质量状态分开；内建浏览器从阶段阅读正文和证据。以明确标记的集成验收运行复用现有不可变资产，验证展示，不将其说成重新执行研究。完整付费重跑不是页面验收的前提。

- [x] 核心 Phase 与绑定、并行持久测试。
- [x] 安全 API 聚合与前端阶段阅读。
- [x] 新研究流程版本接入，旧定义仍可恢复。
- [x] 控制调度与完整性失败原因定位。
- [x] 集成检查、浏览器阅读及剩余限制记录。

### Phase 集成验收记录（2026-09-16）

已落地 scoped `ctx.phase`、稳定实例 ID、嵌套/并行隔离、持久阶段事件与不可变资产绑定。单帖、博主综合及父流程新增 v3；v1/v2 保留注册。控制编排任务不占模型槽，不改变实际模型并发上限。API 按 phaseId 聚合，并保留运行中的已发布子资产；跨博主引用被拒绝。资产正文仍由原有受 SHA 校验的阅读器提供。

内建浏览器验收运行：`82d43297-062e-45de-9b96-21ac11cfe718`，定义 `phase.reading-acceptance@1`。它明确复用两份单帖和一份博主综合候选，没有调用模型、改写报告或晋升候选。可以从阶段直接展开正文，执行记录默认折叠；预期但未生成的验收资产明确显示未产出。它只验证页面与资产传递，不能当作 v3 完整研究成功。

实际读到：CometQ 样本解释了软件远控对系统状态的依赖，但素材没有证明完整安装、网络配置及端到端远控过程；综合候选区分了高低点赞均出现的商业/趋势叙事，不能把包装归因为表现差异，TuyaClaw 的连续配置步骤也不等同于 OpenClaw 的概念素材。证据与未知均可在正文附近找到。这验证了读者能够获取认知与边界，而不只看到任务成功标记。

新默认 v3 暴露恢复缺陷：原父流程 `b19b8cd1-73bd-40d9-954b-c698945b7757` 恢复时解析为当前默认定义，触发 `WORKFLOW_DEFINITION_REVISION_MISMATCH`。修复为按持久 workflowId+revision 查找，不能修改历史记录版本或绕过校验。原父流程及两份计数不一致候选仍须分别确认恢复/修复结果；不得关闭 #71。

检查记录：首轮集成定向 68 tests 通过，构建、lint、包边界、前端边界及源码行数检查通过。`check:repo` 被工作区新增的 `pnpm-lock.yaml` 与 `pnpm-workspace.yaml` 根目录政策阻断，这两份非本轮文件未删除。后续补充阶段用量/链接和版本恢复后仍需最后验证。

恢复实证：通过受限的 `recoverPreExecutionDefinitionMismatch` 恢复原 root；要求 exact mismatch、同 workflowId/revision、相同 frozen input 指纹，保留所有已有步骤。原 run 进入 generation 2，父流程已从 queued 推进到 waiting；数据库观察到 1 个 post.review 和 1 个 post.repair 正在执行。没有重建父任务或把 v2 改为 v3。此时全仓 531 passed / 1 skipped，构建通过；阶段历史计数与多实例父子关系的后续加固仍另跑回归。

最终加固与页面复验：阶段按真实 owner run 与继承 phaseId 组织父子树，同名 sibling 不串位；当前进度只数每个 run/key 的最新步骤，用量包含子阶段的真实模型 attempt，重复 usage 事件不重复累计、缺失用量仍标未知。新增 generic 子流程、同名 sibling、历史失败/成功步骤及缺失 usage 的组合回归通过。耗时依据阶段事件时间，缺失时显示未记录。

资产深链现固定当前运行、phase instance、角色和 immutable artifact ID，多处引用不产生重复 DOM ID；内建浏览器实际点击、刷新后资产仍展开且正文与证据可读，编导 Lens 切换成功。最终构建、定向测试、lint、包与前端边界/源码行数通过。阶段用量已注明含子阶段，不能再把父子展示数相加。UI验收仍是现有资产复用，真实 v3 全博主闭环未在此轮完成，#71 保持未完成状态。

## 本轮执行闭环：v2 收尾 → 新博主 v3 → Host 阅读（2026-09-16）

用户已明确同意以下顺序；不以启动代替完成，不扩大到框架新功能。

1. 收尾 AI硬件情报局 v2 原 run：两份实际失败候选由 Builder 修订，通过独立复核；成功单帖不重复构建；完成综合并核对正式版本登记。
2. 预检一个待完成博主（先检查三次方AIRX），复用实际已有有效源资产，跑完整 v3 父流程，记录真实阶段与中间资产、模型/方法配置及恢复结果。
3. Host 用内建浏览器阅读综合和关键单帖，记录具体获得的知识、证据边界、未解问题及组织缺口。必要修订返回 Builder 或投影层，不能为成功状态伪造研究结论。

状态：第 1 步执行中；第 2 步只读预检，未启动；第 3 步先读已有版本作为比较基线。模型限定 Terra medium + Luna medium，原 v2 driver PID 8476 继续工作。

### 本轮 Host 阅读基线（综合尚未刷新）

实际浏览 AI硬件情报局正式页面的价值、知识、模式、表现和后续问题五部分。当前综合已有具体可学内容：无摄像头眼镜是在公共被拍风险、持续收音与屏显补偿之间作取舍；Wyze 的硬件入口/订阅/渠道、咖啡机器人的场景复制/服务收入、红包的补贴/习惯不是同一种增长机制。拉链的几何锁定、远控的系统状态访问、灵巧手的数据闭环也不能因都谈“约束”就合并为同一机制。

下一版阅读重点：保留这些具体差异，避免压缩为泛泛“差异—约束—价值”；以单帖来源修订后的内容为基础。对表现仍只能说明观察关系：高低点赞均有具体机制或步骤，缺曝光/完播/同窗口信息，不能据此判定哪种包装有效。后续研究不仅是查数字真伪，也包括不同产品的用户、行为和成本该如何比较。

页面已诚实提示两篇单帖来源更新、当前综合尚未重新归纳。新综合完成后应实际检查这一提示与其冻结输入是否匹配；不能只看执行成功。

v3 预检结论：改选“机器人前瞻” (`2224f773-c005-4326-ba14-a3bf42ff444f`)。它有12/12验证媒体、无既有workflow，旧批次唯一失败为评估cue枚举合同错误，适合验证候选→复核→定向修复→综合。三次方AIRX同样媒体齐全，但一份旧候选在OPEN-S3的时间范围2.6–5.8引用了2.5秒帧，当前保留候选机制会再次触发真实范围错误；不把它当无缺口样本启动。此时只做预检，尚未启动 v3。

读者抽查新修订：实际打开 post.repair `abfb6125-d0ae-46cf-adc2-86c3ac5f1a34` 的 immutable candidate `cc196a08-79da-42ae-951d-b5c9e2cdb667`。Comet Q 新稿已从原帧读取支持者/美元文字、70毫米及低功耗字幕，保留“画面可读≠商业真实或功能实测”的边界；OCR失败不再被误等同原图不可读。仍有0:10–0:38等内容时间范围缺口，Reviewer的full_timeline_carrier_sweep仍作为findings保留，不把这篇说成已完全验证。直达资产hash被外层折叠审计挡住的UI问题已修，实际刷新后正文可读；无报告正文改写。

### V2 定向恢复实跑（2026-09-16）

两条 `post.build@v1` 的显式 core-evidence-count recovery 已执行成功（原运行 c2c7dc63 / 4b2c9ec6，generation 2），未重跑其它成功 Builder。两个 post 父流程通过正常 retryStep 恢复，已进入 Luna 独立复核。首篇拉链复核真实发现历史画面可见文字 William Freeman 未恢复，自动进入正常 post.repair；这不属于计数修订失败，也不能直接改为通过。原候选、修订候选和独立评估分别保留。

新增恢复路径及 UI 深链修订后的集成验证：13 文件、78 测试通过；npm build（含类型检查）与定向 ESLint 通过。完整研究质量及 v3 实跑仍在进行，不能据此关闭 #71。

Host 继续阅读 Comet Q 候选的编导 Lens，发现跨 Lens 修订一致性仍不足：内容还原已经恢复网页可见 6,741 名支持者 / $1,155,193，但 opening 6.76–10 秒段仍写“红圈数字…未被 OCR 读取，不能确认”。这是“可见文字恢复”和“外部事实确认”混淆的残留，不能由前端覆盖。此候选仍为 needs_review；后续 Builder/Reviewer 应在一次新增证据后检查三 Lens 受影响引用及 unknown 表述，而非只补内容 Lens。已保留原文。

修订审计：两份计数修订的 reconstruction.json 仅 coreEvidence.covered/total 两路径变化；SDK trace 为 Terra medium，项目方法摘要留档。后续显式 count recovery 将继承的旧评估移动到 predecessor-evaluation/immediate-predecessor 并保存 provenance，避免旧 gate 被当成当前评估；已发布历史目录不改动。新增回归 4/4、定向 lint 通过。

V2 恢复阶段实跑结果：拉链父流程 fac6e42a 已结束为 succeeded（含一次 Luna 评估 enum 合同修复）；飞行宠物父流程 7fc549d2 failed，原因是其 post.repair cb89fe65 产出 dangling reference，既有两轮完整性修订未收敛。监督脚本明确停止在该失败，不推进根综合、不启动机器人 v3。正在检查独立评估到 repair 的资产传递：当前 repair 输入仅 gate ID，而复制目录中的 evaluation 可能属于旧候选评估，必须定位后再恢复，不能继续盲重试。Host 阅读发现的人名遗漏仍记录为质量限制，单帖执行 succeeded 不覆盖 Host 的阅读发现。

当前评估资产传递修复已落地并只读核对真实 pet 运行：repair cb89fe65 经明确父子关系解析到 review step ee8a04c4、evaluation artifact 08652e58，current review 含 8.4–20.94 秒团队背景遗漏。匹配候选 ID、revision、artifact SHA、report SHA；v2/v3 阶段 key 和评估合同修复路径分别测试，缺失/歧义/版本不符拒绝执行。current evaluation/gate/证据帧复制到 review-input，来源摘要留档，旧评估归档。

完整性检查已一次报告所有 dangling refs，真实失败样本只读重算返回四个剩余错误及知识单元路径。52 项相关测试通过；源码行数检查通过。2026-09-16T07:10Z 从原 post.repair generation 2 恢复（finish-v2-after-review-fix.ts），监督顺序为修订→单帖复核→原博主根流程；机器人 v3 尚未启动。

恢复实跑补证：显式绑定当前评估后，飞行宠物 post.repair generation 2 于 07:13Z 成功，BuilderValidation 全部通过。Host 内建浏览器比较原正式报告与候选 74d5fdb3，确认新增 CR-TEAM（8.4–20.94 秒）的团队履历、作者主张边界与公司名未知说明，原中段时间缺口消失。末尾 2:02–2:09 仍保留时间范围提示。修复 SHOT 引用只出现“图片未解析”的显示：现在就近渲染既有镜头代表帧、保留镜头区间与代表帧时间标签；Host 截图实看确认图像出现，不改写 Builder 标注。

07:17Z 单帖阶段落定：9 succeeded / 3 needs_review / 0 执行失败。原根 creator.analyze generation 3 已恢复并进入 creator.synthesize d0a60a9a → creator.build 93d8459e。成功单帖没有重跑，未解决质量项保持独立可见。

### 新综合的 Host 阅读结果（尚未验收）

creator.build 93d8459e 的候选 a127506d 在内建浏览器已读完价值、知识、模式、表现、后续问题。结构和引用可达，但仅五条高度抽象的跨帖结论；多条支持项重复同一句“Builder 记录了机制/场景/取舍”。与本轮先读的原博主版本相比，缺失无摄像头眼镜隐私取舍、Wyze 订阅渠道、咖啡机器人场景复制/服务收益、灵巧手训练数据等具体认知。其“边界清楚”优点不足以抵消过度压缩。独立评估仍进行中；执行成功不等于 Host 内容验收通过。已派只读 trace 诊断，定位实际输入读取与方法执行，避免仅加 prompt 口号。

全量回归：99 文件通过、1 文件按配置跳过；545 测试通过、1 测试跳过。修复后的构建通过。机器人 v3 仍待本轮综合落定和执行诊断后启动。

### V2 收尾与真实 V3 启动

2026-09-16T07:25:03Z，原 AI硬件情报局 v2 根流程 b19b8cd1 已结束为 succeeded（generation 3），单帖 9 succeeded / 3 needs_review / 0 执行失败，综合完成独立评估和登记。Host 尚未接受新综合的研究质量，执行成功不能替代该判断。

机器人前瞻真实 v3 根流程 `2aeea465-5147-499a-8729-0ff5dd1e4196` 已启动；creator run `2224f773-c005-4326-ba14-a3bf42ff444f`。12 份已有候选通过受摘要约束的复用进入新的独立复核，不能描述为新跑了 12 次 Terra Builder。视频模型槽限制为 2，Builder Terra medium、Reviewer Luna medium；后续定向修复和综合由真实服务执行。

首轮实际阅读暴露两个问题：父阶段下有 12 组同名子阶段且没有可见帖子身份，同一候选又以绑定资产/子流程成果重复显示；正在做小范围投影与显示修复。另在机器人单帖 `6a916dbf000000000302829c` 实际读到页头“浙大迎新机器狗搬行李”而正文及帧为“399美元开源鸭子机器人”，正在只读追溯源详情/媒体/报告绑定，尚未把它判定为某层错误。

### V3 真实试跑被来源冲突阻断（2026-09-16）

07:33Z 经正式取消 API 停止 root `2aeea465-5147-499a-8729-0ff5dd1e4196`，子流程亦已停止；保留 12 份复用候选、2 份完成的独立评估及取消轨迹，未启动新综合。12 篇核对中仅 `6a916dbf000000000302829c` 明确冲突。一次新的 RedFox noteDetail 响应仍将同 ID 的机器狗标题/正文与 Microduck 视频 URL 绑定；独立下载与正式视频字节相同。故此不是本地报告串帖，但不能确定上游哪一个字段代表原帖真实内容。原帖内建浏览器核对返回“当前笔记暂时无法浏览”，暂无法取得可靠替代源。不要根据相似标题重绑到别的 ID。

本轮不能宣称真实 v3 完整研究通过，不能扩大到剩余博主或关闭 #71。来源修复后须创建新的冻结输入运行，而不是修改原冻结源哈希恢复旧运行。

综合质量改进：SDK 定向修复显式加载同输入 prior candidate，模型仅写研究字段，程序保留原分类与 postAnalyses；具体性诊断资产绑定候选 hash，独立 Reviewer 判断宽泛引用和重复观察，机械信号不直接等于质量失败。新 helper 与 reuse 方法进入后续 creator 方法摘要。该改进已通过定向测试，尚未通过新模型实跑验证。

隔离已实际登记：sanitized 证据资产 `/artifacts/2224f773-c005-4326-ba14-a3bf42ff444f/source-identity-conflict.ea4df9b6c4ff.json`，原始 RedFox 响应只留私有诊断目录。真实 service.startCreatorAnalysisWorkflow 已验证在此 blocker 下拒绝启动。工作台博主页实际显示“来源身份冲突·研究已暂停”，并明确历史报告仍可追溯但受影响结论不能可靠采用。不是删掉坏数据后假装12篇完整。

Host 阅读机器人基线的具体认知：高表现抓手样本提供多形状包覆、单根卷绕、马达电流感知接触、微型抓蚂蚁及无人机吊桶等不同证据；其对数螺旋归因、260倍承重和行业适用仍是视频作者主张。跨帖报告应连接具体动作任务、控制/感知办法与应用条件，不能把“画面不等于证明”作为知识地图的全部内容。这份认知来自实际单帖正文及近旁画面阅读，而非任务成功统计。新综合仍须在正确源身份下验证是否保留这些细节。

最终验证：全仓 564 tests passed / 1 skipped，构建（含类型检查）通过，包/前端边界、1000行上限与 diff whitespace 检查通过。内建浏览器复验真实 run：已取消阶段固定耗时8分0秒、隐藏过期等待提示、保留已完成候选，重复成果已去重；博主页正确显示12已生成/2已复核/0执行/0失败/12已取消。当前没有继续消耗模型的试跑任务。综合质量修复尚须新实例实跑，源冲突尚须可靠替代证据；#71 不关闭。

## 2026-09-17 共享仓库迁移

上述实现最初位于 `packages/workflow`。当前核心、Codex 执行器和 SQLite 存储迁至 `vendor/agent-workflow` submodule，通过 `@signal-room/workflow` 等公共包接入。业务流程和队列仍在本仓库。参见 [共享源码维护](shared-workflow.md)。
