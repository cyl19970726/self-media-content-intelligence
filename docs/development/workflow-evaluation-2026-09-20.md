# 博主级新工作流实跑与验收

## 使用目标与边界

用户要求先通过完整博主研究和 session-forensics 检查新工作流的有效性、稳定性与效率，再重分析已有博主；后续新博主也纳入同一接入流程。主 Agent 必须实际阅读工作台，不能以任务成功、Schema 通过或 Reviewer 无意见代替内容验收。

首个样本为 AI硬件情报局，沿用已采集素材与冻结选样。这里的完整博主研究是「观察作品库 + 全部深读选样重新构建 + 独立复核 + 跨帖综合」，不意味着逐篇深读作品库的全部作品。旧报告保留，新版本有明确来源关系。

## 交付与验收

| 交付 | 验收依据 | 状态 |
| --- | --- | --- |
| 明确重新构建与复用候选的输入语义 | 批次已有报告时，rebuild 不继承旧候选；默认兼容；回归测试 | 已实现，21 项聚焦测试通过 |
| 首个 creator.analyze 最新版本实跑 | 冻结选样均有独立新 Builder；调用与输入、输出版本一致；最终综合引用本轮单帖 | 基线已取消；固定 v8 对照已完成，混合模型 v9 因用户切换 Luna 已停止；全 Luna 验证17:32启动、18:19取消；1篇完成修订未复核，尚无新综合 |
| 独立 trace 审计 | 目标原文、配置可得性、真实调用、失败链、修订关系、用量和耗时；原始日志留私有目录 | 进行中 |
| 工作台实际阅读 | 三个 Lens 全字段可达，证据就近可读；综合包含知识、模式、表现、反例和研究问题；状态与报告版本一致 | 已读固定 v8 三 Lens、v9 AI 宠物三 Lens 及修订稿；综合待产出 |
| 稳定性与效率判断 | 技术失败和内容问题分开统计；每次重试有原因；没有把减少阅读/复核当优化；不能用一次成功推断普遍稳定 | 待执行 |
| 已有与新增博主接入 | 首个样本验收后按顺序推进；来源身份冲突保留阻塞，未通过条件不强行批量 | 待执行 |

## 评估方法

1. 启动前保存当前报告引用、批次、选样、来源素材和工作流版本；显式配置模型。生产调用限定目标 creatorRunId，避免领取其他博主遗留任务。
2. 检查冻结输入是否覆盖整套业务 skill（引用、脚本、Schema 等），实际模型是否读到相关材料，检查重建时是否携入旧候选。存在文件不等于已经阅读。
3. 按单帖记录来源核对、Builder、Reviewer、修订与登记；区分技术失败、未发现问题、提出意见、已修订未复核。Reviewer 不改写正文，修订需要逐项处置。
4. 综合读取本轮全部深读选样，并核对每条跨帖结论的逐帖支持、反例、分母和不确定性。不把历史批次中的旧报告无说明混入新结论。
5. 主 Agent 在页面阅读，抽查重要断言与原材料，记录实际获得的知识、仍缺的内容、页面不可达信息。审计说明抽查范围，不冒充全量内容验证。
6. 用实际调用数、耗时、用量、失败和有效修订衡量效率。SDK 日志与 Codex rollout 的字段不同；无法解析的指标记未知，不能记成零。

## 当前认识

- 现有最新单帖试跑不等于已有完整最新博主链；历史博主报告包含旧流程和修复流程的产物。
- 启动前发现 `pinResearchInput` 默认从批次继承现有候选，执行器会保留它。因此需要显式 rebuild 语义，才能验证「用新方法重新研究」而非仅复核旧报告。
- 更换素材或事实源版本后，旧综合和旧评估不能自动继承为本轮有效结论。

运行收据及私有审计：工作目录 `.runtime/workflow-evaluation-20260920/`。公开文档只记录必要结论和可核对的版本，不复制原始会话或凭据。

## 本轮启动合同

工作流 root：`7c21ead2-2a8c-479a-b2f9-2889a91d3475`，`creator.analyze@v7`；博主批次：`ae713242-656f-48bf-8f2b-42aa16201e3d`。2026-09-20 15:13 CST 启动。

此次通过现有服务调用：

```ts
await services.creatorResearch.startCreatorAnalysisWorkflow(creatorRunId, {
  candidateMode: "rebuild",
  scope: "available_deep"
});
```

- `rebuild` 重新生成候选；`reuse` 与缺省行为保留旧候选复核的兼容路径。`evaluationMode: "fresh"` 单独使用不代表重建正文。
- 缺省 `scope: "selected"` 只运行规范选样；`available_deep` 运行批次中全部有媒体的深读项目。本轮为 12 篇分层选样加 2 篇补充帖，共 14 篇，防止综合仍混入补充帖的旧正文。规范选样元信息不变。
- 已有报告但缺少媒体时，`available_deep` 明确拒绝启动；不得跳过它再宣称全部重建。
- 队列消费限定 `creatorRunId`；这不是 workflow root ID。初始为两个限定目标博主的 worker 循环；15:38 CST 增加一个使用修正后调度规则的循环。数据库的单帖模型并发上限仍为 2，其他博主历史任务不被领取。旧进程尚未加载新优先级，因此不能把混合运行的时延当作新调度的最终效果。
- source checker 与 Reviewer 为 Luna medium，Builder 为 Terra medium；实际调用另从私有 trace 验证。source checker 完成不等于 Builder 已运行。

## 运行中实际使用发现

1. 博主执行页平铺各帖同名阶段，没有帖子身份，无法判断正在研究哪一篇。已按父子归属分组，显示帖子标题与各篇进度。
2. 博主页实时条显示旧综合的“修订已完成”，同时展示新一轮单帖进度；用户容易误以为新综合已完成。已将本轮进度与此前已登记正文分开标明，新综合完成登记前保留旧正文标签。

截至首次运行检查，669 项项目测试、62 项共享工作流测试通过，1 项项目测试按现有环境跳过。完整 `verify` 曾先发现服务文件超过行数限制（已提取范围策略修复），随后发现资产消费者索引过期（整合阶段更新）。这些检查不代表研究内容已验收。


## 基线运行暴露的问题与干预

- **角色指令冲突**：共享包原来的“选定 operator 文件”模式虽然交付了完整 skill 包，却额外要求读取主 `SKILL.md`，与 Builder 的角色入口发生冲突；真实 trace 证实 Builder 阅读了该入口。共享仓库提交 `67c8d3e` 分开原生 skill 入口和 operator 入口，并将已知包内源路径投影到冻结副本；保留完整 references、scripts、assets、schemas。回归验证通过，当前基线继续使用旧 vendor，下一代运行再切换。
- **调度版本遗漏**：队列的控制步骤识别停留在旧版，漏掉当前 post v7 / synthesis v6。已生成单帖的父流程因此可能在其他 Builder 占满槽位时不能推进。修正后控制步骤可独立推进，并在普通待执行任务间优先复核和修订；仍受原模型并发限制。完整工程 `verify` 通过：682 项项目测试、62 项共享工作流测试，1 项项目测试环境跳过；不代表内容验收。
- **OCR 失败未恢复**：当前基线已落盘的前 4 篇共 165 帧均为 `failed/nilError`。同一原帧在宿主使用相同 macOS Vision 参数成功识别 8 行，说明不是原图没有文字。原 host 恢复函数只检查 frame ID 覆盖，误把完整失败结果当终态；正在修复宿主一次恢复及 Builder 对新增证据的有界消费。历史 creatorRun 的 99 份 / 3693 帧属于更早多轮积累，不能当作本轮样本数。
- **内容还原问题**：Host 已实际读首篇新候选的三大 Lens，抽查已提取画面与字幕。报告将“滑块两个部件”与“三条柔性条带”误判为同一对象的数量冲突；把原帖明确提到的设计参数流程归入未知，导致知识还原缺漏。这些问题封存在私有 Host 阅读记录，没有预先提供给独立 Reviewer，用于观察其自行发现能力。
- **失败恢复语义**：一次模型容量不足发生在候选生成前；输入正常，未进入内容校验，usage 未知。标准 API 先重试 child，但已失败 parent 不会自动恢复，需 child 成功后再重试 parent 的调用步骤；已有两步恢复回归测试。该重试在真正再次调用模型前，随本次系统缺陷干预取消，故不能宣称生产恢复已经成功验证。

发现系统 OCR 缺陷后，通过标准取消入口停止尚未生成的项目，保留已经开始的项目继续独立复核。取消时有一项恰好开始，实际为 **5 篇已开始 / 9 篇未开始取消**。原合同仍是 14 篇，本轮不能标记为完整博主研究，也不能将旧综合当作新结论。没有删除失败尝试或旧产物。下一轮使用新的 workflow revision 与共享包版本，单独记录有效性和时延。

## 后续已有博主顺序

当前静态准备情况建议：单车酒吧搞机社 → 大计算算力炼丹炉 → 铁卫士 → 三次方AIRX。各自已有 12 篇深读选样、12 份媒体和旧报告；这里只说明文件存在，启动前仍须核对身份、内容、冻结版本和没有在途任务。机器人前瞻存在已确认的媒体身份冲突和历史待执行任务，继续阻塞；不能用报告数量覆盖该问题。新增博主沿相同来源核对、选样、重建、独立复核、综合与实际阅读合同接入。


## 基线终止与修复验证

2026-09-20 15:55 CST，基线 root 通过标准 API 取消。最终是 14 次来源核对成功、4 篇候选构建成功、1 次独立 Reviewer 成功，完整博主任务未完成。第一次干预保留的第 5 篇仍在执行时随 root 取消；没有产出新综合。

工程与生产隔离是本轮 Host 的执行错误：基线仍运行时修改了同工作树中的 video executor，后续校验发现 `FROZEN_METHOD_CHANGED`。AI 宠物 Reviewer 的一次实际 SDK 调用结束后因该方法漂移被拒绝，造成了无效消耗；其累计多请求用量为 input 232,803 / output 2,386，不是单次上下文长度，也不据此编造费用。方法校验正确阻止混用版本。后续运行采用独立固定 checkout，开发修改留在另一目录；不放宽冻结校验。

首篇有效 Reviewer 绑定了新候选的准确摘要和来源，并使用 fresh Luna 会话且未修改候选；发现封面承诺状态过强。但其 trace 没有打开引用原图，且未识别 Host 已封存的对象层级混淆和参数流程遗漏，因此不能称为完整多模态复核。已修订 Reviewer 入口和方法：先识别原材料的关键内容再对照正文，视觉判断实际回看原图，区分无法验证的现实主张与可以还原的作者表达。Builder 同步修正 OCR 失败与未知的关系。项目范围 skill 更新不进行全局安装或同步。

修复后的 Host OCR 对独立副本进行了真实验证：原 38 帧 failed / 0 行，恢复后 38 processed / 192 行；第二次调用不重跑。旧失败 artifact 与恢复前后摘要均保存。新增文字必须经过一次有收据的 Builder 修订才能算被消费；修订失败不被静默跳过，Reviewer 仍独立检查具体候选。

新默认版本为 `post.analyze@v8`、`creator.analyze@v8`、`creator.synthesize@v7`；新子步骤也使用独立 revision。旧定义保持可解析，**不意味着旧 frozen input 在 live method 变化后仍可执行**。共享 runner 与 skill-bundle 源码、Host OCR 两个 helper 都纳入方法摘要，版本变化必须新开 run。


## 固定提交对照样本与第二处 OCR 漏项

2026-09-20 16:07 CST，以独立干净 checkout 的 `59937682` 和共享包 `67c8d3e` 启动 `post.analyze@v8`，root `401e8811-f098-443d-992e-3382d0c7f46d`。样本仍是拉链装置帖，便于与已封存的 Host 阅读问题比较。冻结 evidence payload 明确包含 `candidateMode: rebuild`，没有 `reuseCandidate`；轻量 root 元数据未重复这个字段，审计必须沿引用读取 payload，不能只看摘要。

运行中观察到潜在 eligibility 漏项：Builder 的中间取证方案使用 `exact_times` / `before_during_after`，并真实执行了 OCR，18 帧均为 `failed/nilError`。v8 宿主恢复以方案中出现特定 OCR mode 为先决条件，会遗漏既有 OCR 但仍保留 exact-times 的合法路径。不过，不能把中间产物当作最终结果：Builder 在 16:17 CST 将文字核对动作改为 `ocr_review`，16:18 CST 真实宿主恢复已完成，18 帧 processed / 93 行，随后进入有界 Builder 修订。因此本轮并没有最终跳过恢复；前述独立副本 smoke 与本轮真实收据分别记录。固定 checkout 始终没有修改。

开发目录已改为统一判断：已有非空 OCR artifact 本身证明发生过 OCR 请求，再按覆盖、processed 状态及已识别的环境失败判断是否重试；没有 OCR artifact 时才要求明确的 OCR capture mode。不从自然语言猜测，也不对所有 exact-times 取证强制执行 OCR。42 项聚焦测试与类型检查通过；修复要通过新的 revision 与新的实际 run 才能完成生产验证。


本轮真实 OCR 恢复绑定 manifest `b0afcc6cf213b13ce2d1d0d7b7446a6f82162fd08538d1ad5a0842722df78a37`，新增文字摘要 `e876b5302e7f92c0160477b8dc299487b564e0600b781b8c95f7bb52bfe901b5`，修订前 candidate 摘要 `b21b9578688a03bec56951464f4d2a2e73e8b78c1f21992d4c91e2758202c54f`。截至记录时 continuation 为 pending，尚不能宣称新增文字已经被正文正确消费。


后续开发版默认入口提升为 `post.analyze@v9` / `creator.analyze@v9`，只将本次受影响的 video 执行步骤提升为 `post.build@v3` / `post.repair@v4`；来源核对 v3、独立复核 v4、综合 v7 保持不变。旧定义保留解析，不放宽冻结方法校验。当前仍运行固定 v8 对照，开发目录不参与其执行。版本与 eligibility 整合后完整工程检查为 709 项项目测试、62 项共享测试通过，1 项环境跳过；尚不代表完整博主验收。


固定 v8 对照的候选构建与 OCR continuation 已完成，独立 Reviewer 已提出问题并进入修订。Host 实际阅读三个 Lens，确认材料与参数设计链条已具体恢复，滑块/条带的数量层级错误未复现；抽查88秒原帧可读“PLA和TPU塑料”。仍发现内容时间段缺漏、已恢复人名与 unknown 字段不一致，已在未读取 Reviewer 意见前封存阅读笔记，待对照复核覆盖面。

工作台还暴露了真实展示缺陷：OCR artifact 已存在，但读取模型没有把 OCR 行加入 evidenceIndex，导致三个 Lens 的 OCR 引用显示“来源未解析”。开发版已补入逐行文字及同一报告内原帧链接，不修改 Builder 正文。OCR continuation 同时补上修订前候选的精确私有副本和路径，失败或备份被改时仍保留原字节；v8 的旧收据只有输入摘要，不能据此伪称已经拥有完整修订前后文本比较。


独立 trace 审计确认本轮 Reviewer 使用 fresh Luna 会话，绑定新候选精确摘要，实际打开2张原图且未修改候选。它自行指出耐久测试与早期失败/制造障碍历史链条两项重大遗漏，并未接收 Host 封存问题清单。它先同批查看候选摘要与来源摘要，再读完整字幕，故不能称为完全盲读 source-first。开发修复整合后的最终工程验证：711 项项目测试、62 项共享测试通过，1 项现有环境跳过，2 条既有 lint warning、0 error。


## 完整 creator v9 验证启动

2026-09-20 16:30 CST，root `0d0f376e-249b-4c52-85c8-57c632c34a55` / `creator.analyze@v9` 在新独立固定 checkout `6e5793fb` 启动。14 份冻结输入均沿 artifact payload 确認为 rebuild 且没有 reuseCandidate；即使拉链帖刚完成对照，本轮也重新构建，确保跨帖综合依赖同一批次的新单帖链。并发模型上限仍为2，三个 worker 循环只领取该 creatorRunId。

此前 v8 对照完整耗时19分11秒，经历来源核对、Builder（含一次Host OCR恢复及消费）、独立复核和一次修订。累计多请求用量为 input 4,304,616 / cached 3,924,224 / output 49,536；不是唯一上下文长度，不据此推算价格。最终状态为 revised_unverified，单帖功能闭环完成，不代表修订稿已再次独立通过，更不能据此宣称批量就绪。

新 API 已切到固定 v9 checkout，Host 在真实工作台验证 OCR 引用从“来源未解析”变为“查看来源”，并打开修订稿材料帧成功；Builder正文未由前端改写。完整博主综合仍未产出。


## v9 首篇闭环与恢复问题（2026-09-20 17:02 CST）

14 篇来源核对全部完成。AI 宠物帖 `6968c3c9000000000b013f4d` 的新 Builder、独立复核及修订已完成，父 run `f3efacca-4a6a-4a14-9a00-06ad69234a2e` 已登记修订候选。OCR 从 37 帧失败、0 行变为 37 帧处理成功、153 行；有精确修订前候选副本、摘要与一次 continuation 收据。Host 在工作台确认公司/产品名、部件与工程风险、传播/电商/资源互补论证已具体呈现。

独立 Reviewer 绑定本轮精确候选，读取完整 95 条字幕并打开 5 张原帧，但先读了候选，并非严格的 source-first；未单独读取完整 OCR。它指出悬停靠近绕行、生气时远离两项行为遗漏，修订稿已补充，最终候选 payload SHA 为 `5589bb89233f80596dabe2ac2065da890cd44074e9694fc0f8e51e602636a5d7`。页面正确显示“修订稿尚未再次独立审阅”，没有把单轮修订冒充二次独立通过。Host 在读取 Reviewer 意见前已封存的团队履历、桌面/地面宠物对比遗漏仍未覆盖；因此首篇流程闭环不等于语义完整，更不代表批量就绪。

拉链帖第一代 Builder 正常结束模型回合，但连续文件写入失败，未生成 `reconstruction.json`，宿主以 `POST_CANDIDATE_INCOMPLETE` 正确拒绝发布。实际错误含 `TextEncoder is not defined`、`btoa is not defined`，不是模型容量或超时。已通过标准 API 申请一次子任务重试，保留旧失败物证，未人工代写 Builder 结论。

重试还暴露了跨代状态读取问题：execution envelope 已是 generation 2 queued，而 core run 尚保留 generation 1 failed；旧父唤醒把后者当成当前结果并失败。重试排队不等于重试又失败。修复应让 adapter 在子 execution 非终态时优先使用该状态并忽略 core 旧 error/output；共享 runtime 的失败传播合同本身正确。当前已经失败的父实例需在子候选真正成功后按标准步骤恢复，不能假称补丁已自动修复这次历史状态。开发修改与固定实跑目录隔离。

### 审计仪器的纠正

outer SDK events 没有 `view_image` 项，不能据此断言模型没看图：嵌套 `functions.exec → tools.view_image` 在原生 rollout 中可见。审计沿实际 `thread.started` 定位 native trace，确认 v9 首两篇 Builder 均有 6 次成功看图调用与图像输入；固定 v8 canary 的完整 native thread 也是 6 次去重调用（3 次单图、3 次多图批次），不是早期局部统计的 3 次。工具调用和返回不重复计数。Host 曾因 SDK 视野不足短暂暂停领取新任务，补全物证后恢复；未为该错误判断修改当前冻结方法。SDK 缺席只能记为未知，视觉读取结论必须由实际配置与 native 证据共同支撑。

恢复竞态修复已在开发目录完成：先用回归测试复现“子 gen2 已排队、旧父 wake 仍导致父失败”，修复后父保持 waiting，子 gen2 成功时沿现有 wake 自动完成；原有真实失败传播和历史失败父流程的标准恢复测试仍通过。完整工程验证为 712 项项目测试、62 项共享测试通过，1 项环境跳过，2 条既有 lint warning、0 error；Web/API/Worker/CLI 入口检查通过。当前固定实跑仍使用启动时版本，此修复没有热更新进去。

### 阅读诊断修复

Host 阅读发现 AI 宠物 `CR-UNKNOWN` 的全片时间范围掩盖正文空档。开发版仅在覆盖计算中排除 schema 显式 `type: unknown` 的块；仍完整渲染该块，不按标题或文字猜类别，也不改写 Builder 内容。在独立前端预览连接真实 v9 API 后，页面正确提示 0:27–0:37、1:11–1:17、2:02–2:09 未被正文时间范围覆盖，并保留“是否遗漏内容需回看原视频确认”的边界。时间诊断不是语义完整度判定。


## 用户要求统一 Luna（2026-09-20 17:25 CST）

用户明确要求所有模型使用 Luna。先停止领取新任务，等待已执行中的两个 Builder 收尾，17:24 CST 通过标准 API 取消旧 root，保留已有候选、失败与修订产物。该混合模型 v9 最终有 14 次来源核对、2 篇合格 Builder 候选（AI 宠物、无摄像头眼镜）、1 次 Reviewer 和1次意见修订；远控硬件和拉链 Builder 失败，未产出完整博主综合。眼镜候选在取消后仍可在工作台阅读，明确显示已取消、复核状态未知；没有把历史批次报告伪装为其新结果。

远控硬件候选的残留错误是 `KU-NAME-PROBLEM`（28.36–37.36秒）引用7.5/8.5秒的OCR与原帧。早段证据能支持产品名，但按现有合同应留在早段知识单元，通过跨单元关系解释与后段ASR的差异；不扩大时间范围、不放宽校验来换取成功。候选保留，未人工代写或发布。

模型切换暴露出配置未接线：旧 SDK 路径只读取 executor options，而工作流未把角色声明的模型传下去，单改环境变量不会改变真实调用。开发版已将角色模型及推理强度传至 post/creator SDK executor，统一 Luna 默认；保留显式覆盖的兼容性，来源核对和结构评估修复维持其既有固定 Luna 合同。图文和报告总览的默认值同步为 Luna，项目本地配置与 AGENTS 已记录用户偏好；未改变全局设置。完整模型变更检查为 718 项项目测试、62 项共享测试通过，1 项环境跳过。

旧 root 取消时，core 仍显示上一代失败的拉链重试子任务未被递归取消。已通过标准 API 单独取消该 queued generation 2，并经限定 creatorRunId 的公开服务清理终态队列收据，确认目标无 queued/leased/running 任务；没有直接改数据库。此取消传播缺陷另加回归修复。原来的待重试记录不能被算作已成功生产恢复。

下一轮使用新固定 checkout，全部14篇显式 rebuild，并以实际 trace 核对 Luna；不会把前面 Terra 候选混入全 Luna 质量/效率结论。


## 全 Luna 固定运行启动

2026-09-20 17:32 CST，root `d83d1cc0-b948-4fb9-8762-22a057c4c4f8` / `creator.analyze@v9` 从固定提交 `86fa02b4`、共享包 `67c8d3e` 启动。14份输入均实查为 rebuild、无 reuseCandidate、23项方法摘要。API与worker都来自该固定checkout；开发目录后续修改不进入实跑。三个限定creatorRunId的worker循环继续遵守SQL模型并发上限2。当前只确认已启动来源核对；实际模型由逐调用trace复核，尚无本轮综合。

取消穿透已失败父任务的修复通过真实形态回归：失败父仍保留，queued generation 2孙任务被取消，旧失败attempt和artifact保留。整合后的完整检查为719项项目测试、62项共享测试通过，1项环境跳过，2条既有lint warning。旧混合模型v9的启动前22份产物在取消后逐一摘要比对，22/22未改变。

取证配置另有一项认知校正：设置 `SELF_MEDIA_CODEX_EPHEMERAL=true` 不等于SDK会话临时化；当前SDK路径不传该CLI flag。native会话实际存在，fresh独立性依然成立，但不能声称未持久化。同会话OCR续跑需要明确的线程、角色、输入/方法摘要与次数约束，目前只是尚未验证的优化候选。Vision的host/sandbox差异有同输入A/B证据，尚无具体deny/XPC日志，不能把底层机制写成唯一已证实根因。


### 全 Luna 首批调用取证

14次来源核对已全部完成，实际usage共17次（11个核对各1次、3个各2次），模型均为Luna/medium。首个来源核对的原生会话与workflow收据一致；输入5张图片的摘要及规范化inputSha256均与输出绑定相符。来源判断明确限于原帖与视频身份，未将视频中的市场或技术主张当作外部已证实事实。

前两篇Builder的实际调用同为Luna/medium。首篇原生trace证明读取了冻结Builder operator与single-post-depth，并有6次view_image调用；完整skill包已交付，但不据文件存在推断全部被读取。该时点正文尚未产出，不能作质量或完整博主成功结论。

审计的第一次探针只查询root事件，误把父任务waiting理解为尚无模型调用；现已沿workflow_runs.parentRunId和research_workflow_executions.parentWorkflowRunId递归纠正。工作流UUID与Codex线程ID不是同一标识，原生trace须沿真实thread.started定位。审计工具本身也需要用实际子任务物证校准。


### 首批 Luna 输出与进度页面验证

首两篇Builder均因builder_integrity_derived_source_missing失败：候选分别把媒体清单和原帖身份文件的内部字段片段拼进derivedSources.path，而合同要求真实相对文件路径。每篇两次有界结构修订后仍未通过，原候选保留，不能登记为合格报告。Host已暂停领取新的工作，允许当时正在构建的远控硬件与眼镜收尾；本轮仍未形成完整博主综合。

Host在独立Reviewer之前阅读AI宠物初稿并实际查看5张冻结原帧，发现耗电高被写成重量高、清楚可读的团队履历被笼统记为未知、悬停/靠近/绕行未完整进入内容正文；桌面/地面对比、情绪例子、商业解释和市场边界已有还原。这是初稿证据，不能扩展成全部Luna能力结论，也不能把失败前的候选当作最终已发布版本。

实际工作台还发现执行中帖子排在排队列表后、阶段等待状态遮住了运行中的子流程。开发版已把运行中与需处理的帖子提前，同级保持原顺序；等待阶段只有在read model存在真实活动子流程时才显示其状态，并展示对应子流程名称。完整verify通过。隔离前端预览连接真实API后，确认两篇运行中优先、两篇失败紧随、候选构建明确显示执行中；冻结worker与API未热更新。


### 全 Luna 暂停结论与下一轮修复（2026-09-20 18:20 CST）

这次固定运行最终有14次来源核对成功；4篇启动Builder，其中眼镜通过构建后完成独立复核和一次修订，AI宠物与拉链未通过完整性校验，远控硬件取消，另外10篇未启动构建。眼镜父流程 `b8841dab-5c56-4d73-97ea-466f77b5e3e3` 成功只表示单帖流程结束，最终 `revised_unverified`，并非修订稿再次独立通过。其修订候选payload SHA为 `bf013981aafbc6a86b2db0e67ee511cc8e97799b98bb7eb940af28df2ca9e09d`，依赖本轮Builder与Reviewer；未生成博主综合。

原生trace纠正了对失败原因的粗略归类：AI宠物两次修订提示均指向 `post-source-input.json#/facts/title`，首次模型误认为无须修改，第二次修正了该路径，但留下未被提示的 `media-preparation.json#/sourceMedia,/audio`。拉链的两次提示分别是悬空帧引用和核心证据计数；模型修好了这两项，最终的 `post-source-input.json#facts.title` 路径错误才露出。并非两篇都反复忽略同一个路径问题，也不是文件权限或工作目录导致。这说明只返回首个错误的校验方式浪费了有限修订机会。

远控硬件的原生进度探针发现三次巨型内联Python写入均因括号语法错误失败，尚无正文文件。通过标准入口取消该子任务，保留失败记录；未人工代写。等待眼镜独立复核与修订结束后，再通过标准入口取消旧root，并用公开服务清理10项终态队列收据，剩余在途任务为0。启动前22份旧产物摘要逐一比对，22/22未改变。

开发目录的修复保持两次完整性修订上限和全部既有验收要求：

- 冻结媒体/证据/封面校验仍先行拒绝；可信输入内可独立检查的候选错误集中返回，模型能在同一次修订看到路径、引用和计数等问题。
- 明确文件路径、已注册证据ID、字段说明三个字段的不同含义，Schema通过不能代替宿主完整性检查。
- 每次完整性修订保存触发错误、精确修改前后字节、摘要、时间与模型子调用标识；回调完成不代表校验或质量通过。旧运行没有这份新增收据，不能倒推拥有历史完整快照。
- Builder写入指引采用标准序列化及小块检查，重复语法错误必须换方法；保护原始ASR不等于正文照搬错误，可读字幕需要实际核对与就近证据。
- 方法摘要补入宿主完整性、深度校验、组装和修订收据四个实现文件；新增运行应冻结27项。该清单不是全部传递依赖覆盖的宣称。

整合后的完整 `verify` 通过：734项项目测试、62项共享包测试，1项现有环境跳过，2条既有lint warning、0 error。代码通过不等于内容通过；先在新的固定提交上执行单帖Luna对照，再决定是否重启14篇完整博主验证，避免直接扩大失败批次。


眼镜独立复核的原生线程 `01a0be4e-9c0e-7a33-863c-8d4317357cdc` 确认 Luna/medium，并精确绑定初始Builder payload与报告摘要。它读了OCR和证据包，首次图片路径失败后改正目录，实际打开6帧；发现可读日程条目被笼统当作乱码，两处major finding指向具体JSON字段。修订稿SHA `14f198f72127a54c0a7526ec7f171037f9cb8ae3a0deddbf192f219e87a8fdf1` 已补入三项日程及时间。单轮复核策略本来允许 `revised_unverified` 结束，缺二次Reviewer不是执行违约，但不能因此宣称修订稿质量通过。

Host另行阅读了眼镜修订稿三Lens：主论证顺序可读，画面载体和未展示的操作边界完整可达；但清楚可见的蔡司近视镜片仍未进入内容还原，研发金额被加上未经证实的年度范围，且编导分析把ASR错字当成观众理解成本。新增日程条目并未解决这些独立发现。原始ASR与大量无说明帧直接铺在正文附近也增加阅读负担；这与Builder正文缺漏是不同问题。声音作用分析尚未产出。上述范围说明保存在私有Host阅读记录，不将一次看图或review成功当作内容完整。


### 修复后的Luna单帖对照启动（18:25 CST）

固定提交 `5e523c37`、共享包 `67c8d3e` 启动root `d5675fea-24d0-4668-a710-aab228a395c9`，拉链帖显式rebuild；该运行不混入旧候选。来源核对已完成，Builder已进入执行，尚不能作结果判断。API也切到此固定版本；前端开发预览单独运行，没有改动模型运行中的源码。

单帖页另修正等待文案：它的DTO只有父阶段状态，并不含实时子流程状态；原来把waiting固定显示成“子流程执行中”，会在child已经结束而父唤醒尚未处理时误导。现在如实显示“等待流程推进”，不猜测child状态。真实API连接的隔离预览已确认该文案和“当前显示批次报告；候选版本尚未可读”，旧报告身份仍明确。组件7项测试通过，整合verify仍为734项项目测试、62项共享包测试通过，1项环境跳过。
