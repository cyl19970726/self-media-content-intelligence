# Restore previous creator and post pages

User rejected rebuild 375b02a9. Restored pre-rebuild frontend 38839005, retaining deletion of creation/publishing frontend files and route/navigation. This is a restoration, not continuation of the rejected design. Existing model outputs, runtime data and personal local-storage records were not modified.

One existing layout defect was corrected for usable restoration: the newly wrapped knowledge contribution `details` did not span the creator grid, placing the directory in the content column. The audit wrapper now spans the grid. No content reorganization was applied.

## Evidence reviewed

Real browser on 5177: creator AI红发魔女, run `5c8dc101-30b6-4c38-a900-08fb6ce1c2f0`, all creator sections; corresponding posts `69424c0d000000001e039745` and `690ac8730000000004014f67`, including original content and directing view. Independent read-only agent checked original source and API data. This is not a claim to review every creator or every video.

## Findings, not implementation proposals

1. Run progress and expanded 13-stage pipeline appear before creator identity. Worker/gate/artifact vocabulary dominates the first screen.
2. Existing `run.publicProfile` bio, followers and profile post count are not shown in the identity hero. This snapshot has profile count 341 versus 186 collected works; show both source/time/scope rather than treating collected samples as account size.
3. Structured percentiles and distribution are missing in dossier projection while source-linked text already states quartiles. Topic/format cluster arrays are empty while textual analysis contains theme counts. This is a projection gap plus UI emphasis problem, not grounds to fabricate chart values in frontend.
4. The 21-row works table repeats `topic` as `coreContent`, and 21/21 contentArchitecture arrays are empty. Selection role, performance, format and evidence links remain useful and should be retained together.
5. Legacy post thesis says Builder did not produce content reconstruction, while saved Builder article is fully displayed below. The fallback confuses absence of new schema with absence of analysis.
6. Both sampled articles contain substantial English content and audit language. New lenses and overview fields are absent; equal-weight tabs lead to mostly empty directing/visual views. Content quality/language/format must be distinguished from CSS problems.
7. The existing full creator outline, same-record sample roles/metrics and direct post links, continuous original content and nearby evidence are valuable. They should not be dismantled into disconnected generic cards.

Screenshots: ignored `.runtime/restore-review-20260914/`. Remaining findings are intentionally not fixed in this restoration/diagnosis turn.
