# 新帖从零工作流验证（2026-09-18）

## 使用问题与边界

上一轮拉链报告只验证了既有候选的独立审阅、修订及页面阅读。本轮选择从未生成过报告的帖子，验证首次构建、审阅和报告页随进度更新。

- 博主：AI硬件情报局。
- 帖子：`69296e94000000001e00e9a6`，《Meta大牛离职造“魔戒”狂揽近亿元融资》。标题属于来源身份，不视为事实核实结论。
- 研究批次：`ae713242-656f-48bf-8f2b-42aa16201e3d`。
- 新流程：`0f223038-0520-4e56-ab9d-e416d7a7af00`，`post.analyze@v5`。

## 启动前证据

现有媒体就绪的73条批次记录都已有报告，因此没有复用其中任意一条来冒充新帖。选中帖子原先只有基础采集记录；准备时检查无旧 reconstruction 及工作流 post-candidate。

重新取得详情并校验帖子身份，下载视频后经现有媒体核验器确认完整：115.471383秒，10,792,601字节，720×1280，含音轨；SHA-256为 `dfc27941730e2f5a9374ff0a2c69fdae34d21b2a070c72143e3174075c6e5183`。

准备过程保存原引用快照，新增版本化详情、媒体清单及批次r47。保留12条已有报告，新增目标项的 reconstruction 为 null。准备步骤通过本机脚本完成；媒体采集尚不是报告页按钮内的一体化步骤。随后由主 Agent 在内置浏览器点击“用当前方法开始研究”创建流程，限定 worker 只执行此研究批次。

私有收据、提供方原始响应、模型事件及媒体留在本机 runtime，不提交到仓库。

## 工作台发现

原页面在没有批次报告时会提前报错，无法进入新帖工作流。已将“没有报告”与请求失败区分，独立读取研究进度；首次候选登记后使用原有 Builder 阅读投影显示正文，不构造虚拟报告。来源核对、生成、审阅、修订的记录仍绑定本次实际运行。

## 验收状态

实际执行已完成：北京时间21:19:46至21:36:16，约16分30秒。来源核对、首次构建、独立审阅均终结；工作流树中 `candidate.copied` 事件为0。

- 来源核对子流程 `952f3fd1-9878-43b9-8d0b-e1711dadf411` 成功。
- 首次 Builder 子流程 `8749e270-f4a5-4fad-bad6-5861d2358a8a` 使用 Terra medium，真实执行约11分钟，生成全新候选 `a6d07ba2-fbd3-405b-8654-43ed0fd3517f`；报告 SHA-256 为 `ffd99fc405f28711c36dbb32c100812775188475d9f266ba2da39f19b5edac12`。
- Builder 的 skill 快照包含44个文件，其中13个脚本、12份references和5份schemas；不是只有入口Markdown。包摘要为 `695dfd747dcabd2cce7972af0d42012763f69821a0ae283659c2c5ee1b10ad15`。
- 浏览器未刷新，空白等待页自动切换为首稿与“独立复核”第3步。活动阶段显示“子流程执行中”，禁用重复启动按钮；候选短暂读取失败会有限重试。
- 主 Agent 实际阅读内容还原、编导结构和画面剪辑，使用目录定位商业模式证据。正文能说明按压语音笔记定位、灵感流失的痛点链、Inner Voice作者主张以及硬件价格/订阅条目；编导分析区分钩子、痛点、功能高潮和商业收束，画面分析区分字幕主张和B-roll示意。
- 首稿如实保留OCR失败、竞品拼写及非语音音乐语义未知；不将作者融资与功能宣传当作外部核验事实。机器转写错误仍可在旁边的原始证据中看到。
- 第一次Reviewer提出“价格和预计发货不能自动证明预售”的修改意见，但location将三个JSON Pointer以分号拼接，未通过严格位置校验。该失败及原输出保留，流程自动执行第二次审阅；不能将格式拒绝描述成审阅通过。

第二次审阅 `20489605-acb8-4d5c-9761-10aca762df39` 返回空 findings，产出有效审阅资产 `4e9417bd-e978-44bf-91d1-cff007a14ab2`，因此流程成功结束且未触发修订。首稿就是本次交付版本。

这验证了从零执行和候选自动展示，但不证明报告没有内容问题：第一次审阅提出的“预售”语义扩大仍值得处理，而第二次未复现该意见。Host没有改写或清洗任何一次原始输出，也没有用格式校验成功冒充内容问题已解决。下一步研究质量改进应聚焦审阅失败后的有效意见保留与一致性；当前已定位的工程缺口是重试未带入具体失败反馈。

## 验证检查

空态支持的完整 `npm run verify` 通过：宿主655项测试通过、1项跳过，共享workflow40项通过；包括类型、仓库边界、lint、构建和入口检查。活动状态文案、重复启动保护及读取重试补丁之后，8项针对性测试、类型检查和Web构建再次通过。终态投影与审阅重试反馈修复之后，完整 `npm run verify` 再次通过：宿主660项通过、1项跳过，共享workflow40项通过；lint零错误，有2条组件文件导出辅助函数的Fast Refresh提示。


## 实跑后的修复

- 报告页支持没有旧报告的空态与首个候选自动显示；运行中不能重复启动，父阶段等待其子流程时显示正在执行。
- 审阅状态由精确绑定候选id/revision/sha和报告哈希的有效review资产计算，避免报告始终显示pending；旧Evaluator路径保留其版本语义，修订稿不继承原稿审阅。
- 新版重试携带第一次的具体失败原因；输出约束要求每条finding一个真实JSON Pointer，并提醒格式失败不是删掉实质意见的理由。合法属性名中的分号仍可用。
- 新默认定义为 `post.analyze@v6`、`creator.synthesize@v5`、`creator.analyze@v6`，使用 `review@v3`；旧父流程及 `review@v2` 继续注册。本篇实际使用的是v5；新重试机制经回归测试验证，本轮没有另行付费重跑并冒充同一次执行。
- 再次逐项核对批次r48与启动前快照，原有12条报告记录完全相同，只有新帖增加报告。
