# 提问如何改变验收行为

使用 session-forensics 对当前会话做有界复盘，关注用户提问前后的观察方式，而非重新分析业务报告。日志位置：`~/.codex/sessions/2026/09/06/rollout-2026-09-06T08-03-46-01a07407-3be8-7c10-8846-fbb50c43d033.jsonl`。下文 L 为该日志行号。原始 JSONL 未复制入项目。

## 目标合同

- L8871 用户：“现在，我觉得你需要作为用户来读一下这个报告。”以及“还有哪些认知，以及还有哪些疑问？”、“认知的摆放是否合理？”要求是实际使用后判断理解与组织，不是直接执行新一轮重写。
- L8945 用户：“很好开始下一轮。”才进一步授权按照阅读发现开展修复。
- 本次用户要求解释这组发问触发了什么；不能把后来的修复全部归因于第一句角色提示，也不能把 Agent 的内在机制说成可直接观察的事实。

## 执行偏差与变化

L8850、L8864 的交付叙事强调五部分、十条发现、引用与入口，以及已经做过用户 Agent 阅读。因此“此前从不使用浏览器或从不安排读者”不成立。可确认的变化是验收关注点发生了变化。

L8875、L8880 主 Agent 实际打开综合页并提取正文，随后 L8887 起进入单帖。L8925 读取智能体帖编导部分，L8932 回到内容还原检查五项能力是否展开。观察从结构与可达性延伸到了能否具体复述知识。

解释性假设：用户的发问提供了新的验收问题，促使主 Agent 从交付者转向成果消费者，并把自己的未解问题当作调查起点。日志证明动作变化，不证明它是唯一原因；任务阶段、已有材料与后续修复授权也参与了结果。

## 错误链与实际代价

一条可定位的链：主报告在认知层仍有不足 → 阅读后发现信息压缩与重复 → 修订页再次实际使用 → L9512/L9519 检出投影层把整段 `block.body` 填成逐帧 `proves` → L9525 删除这类自动补写 → L9536 回归测试通过 → L9553/L9556 实际打开六篇并检查重复图注消失。

这说明可读性问题不一定只是美术问题；它可能暴露来源到展示之间错误的职责分配。代价至少包含一次本可更早进行的投影修复和页面重验；没有可靠工时归因，不编造节省小时数。用户此前得到的是重复说明，而不是每张图真正独立的支持范围。

另一个有边界的反证：本轮修订候选也需要独立 Reviewer，不能因读者发现有效就相信修复自动正确。局部复核记录见 `.runtime/knowledge-repair-20260914/independent-review.md` 与绑定候选指纹的 `review.json`；曾检出主板段时间范围与技术缩写规范化问题，修正后才登记。读者视角不能替代证据复核。

## 升层点与后续动作

应升层的位置是在宣告可用于研究讨论前，把“能打开、字段齐全”进一步检查为“能回答实际使用问题”。这里缺少的是验收层次，不是又一道用户许可；用户目标已足够明确，无需额外暂停询问。

下一步将“验收问题决定观察行为”写入通用心智模型。继续沿已确认方向，由主 Agent 实际使用成果、组织具体修复或综合任务，再回到使用检查；不因为本次复盘额外重跑所有 Builder。

## 取证限制与自检

本次定位并测量整份日志，只钻取三个有界窗口（L8845–8894、L8920–8969、L9510–9559），其余仅做关键词定位，并对照当前产物。部分记录被有界截断，未声称逐字重读全会话或所有子 Agent 日志。

校准脚本向项目运行目录写入十文件样本，保留样本为零；不能解释本地分位或作会话好坏的统计判断，本次未使用这些数值下结论。也不把一次案例描述为跨会话稳定规律。

自检五问：未认定新洪水源；未修改统计阈值；校准零样本作为限制保留；未建立跨会话复发证据；此前“减少审计重复”的建议必须以源结论、必要边界和证据仍可获取为下限。局部发现只进入本项目记录与用户要求的思维 skill，没有修改或传播全局 session-forensics。
