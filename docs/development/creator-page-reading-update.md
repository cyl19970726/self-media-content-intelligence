# 博主页面阅读更新

目标：先认识博主、理解实际研究状态、阅读作品，再查看有证据的综合分析。

原则：保持恢复版视觉；不补写分析结论；区分来源事实与综合分析；排队不等于执行；已有素材可在本地阅读；研究审计默认折叠。此轮不启动模型分析。

交付：前端阅读顺序和空状态优化；后端已有统计与本地媒体投影；真实用户 Agent 验收。

验收：首屏有博主来源事实；13阶段不展开；列表和图库能比较点赞；可读本地媒体；新报告仍独立；已有统计不丢失；测试、构建通过。

## 完成与验收

来源事实、研究状态与作品库前置；已有定位及逐帖核心内容/结构/机制保持可达。未产出章节集中说明，技术审计在 DOM 与视觉顺序中均置于正文末尾、默认折叠。列表/图库保留比较指标，已采集正文与12条本地视频可展开阅读。

后端直接投影 P25=3、P75=28.25；按全量表层标注计数形成主题/形式统计、视频数和分布，注明不替代深度结论；不把7条高表现选样外推为全量高表现数。本地视频只透传同运行已验证的 artifact 路径。

验证：全量72个测试文件、378项通过；新增组件和投影定向测试5项通过；最终作品分析可达回归通过。构建、类型、Lint、仓库边界、文档检查通过。

独立用户 Agent 在浏览空间40复验：首屏事实和排队状态一致，高表现/图库切换有效，21个唯一作品无重复，目录目标均有效。首条本地视频实际播放（readyState=4、time=0.575秒、error=null）。技术审计与知识记录默认关闭。原始截图存于本机 /tmp/creator-dossier-updated-*.png。尚有图库展开后较拥挤等非阻断体验细节，可在真实内容研究中继续迭代。

未启动模型分析，未改写 Builder 结论或删除素材。

## 研究进度去卡片

按用户进一步反馈，移除重复状态卡、13阶段卡片及“部分研究投影/7\u002f13”完成度文案。主页进度改为六行阶段列表，依据 run.currentStage 与执行/排队记录区分完成、等待、运行、失败、跳过；13项检查仅保留为默认折叠的次级证据表格。当前明确停在第四步单帖分析：12条等待执行，后续博主综合和交付未开始。

用户 Agent 在空间41复验六阶段、文字去重及明细折叠均通过，截图 /tmp/creator-progress-text-acceptance.png。本轮全量386项测试通过，构建、Lint、仓库检查通过。仍未启动实际分析任务。
