# 2026-09-14：工作台仅支持最新单帖报告

## 决定

用户明确要求只保留当前 Builder 三部分格式，清理旧分析数据及兼容代码。保留恢复后的页面风格，本次不进行视觉重写。

主报告事实源为 `reconstruction.json.builderLenses`：内容还原、编导逻辑、画面与剪辑。API 不回退到旧 Markdown 或其他运行的报告；未知作者的独立单帖使用运行身份，不能归到无关博主名下。

## 数据清理

主运行库 `/Users/hhh0x/self-media/.runtime` 已删除 21 个旧博主研究注册、5 个旧单帖注册、1,584 个旧报告/评估/汇总文件（27,135,825 字节），及依赖这些运行的批次、比较记录。最新版 `00000000-0000-4000-8000-000000000908` 已从工作树导入主库。

保留“单车酒吧搞机社”首次研究运行 `1daadd8d-5921-475b-8b44-0232cede33d1`：12 个待分析任务未启动。旧分析衍生的另外 9 个知识版本通过正式失效接口撤销；历史发布绑定与审计仍保留。独立手工知识不属于旧报告删除范围。

原始视频、图片、转写、采集证据保留。其删除范围已询问用户，尚未确认。外部证据归档和未注册的历史实验目录不作为工作台报告来源，本次未整目录删除。

清理脚本先生成逐文件 SHA-256 清单，执行前验证；复制最新版目录允许恢复一次已复制但未注册的中断，前提是全部文件哈希一致。实际计划、执行回执与知识失效回执保存在工作树 `.runtime/latest-format-cleanup/`。脚本是针对本次盘点的一次性迁移，不是通用格式升级工具。

## 验收要求

- 单帖列表只展示最新格式，能够进入真实三部分报告。
- 旧单帖 HTTP 接口返回 410，旧博主/帖子链接不回退到归档数据。
- Builder 全字段、连续内容块及相邻证据仍可达。
- 主库只剩最新独立单帖和待研究博主；不启动全局分析或发布 worker。
- 检查构建、类型、测试、仓库边界，以及真实浏览器阅读。

## 验收结果

- 全量测试：70 个测试文件，373 项通过；最后的比较路径及首页接口改动追加 3 项定向测试通过。
- 构建、类型、Lint、仓库边界、文档链接、Web/API/Worker/CLI 启动检查通过。
- 浏览器实测最新列表 1 条；内容还原 5 块连续呈现；编导 6 阶段可达，25 张图片无加载失败；画面与剪辑 46 张图片无加载失败。
- 旧报告 URL 返回 404，旧单帖接口返回 410。独立单帖返回单帖列表，首页正确计数为 1 单帖、1 博主。
- 删除旧 CLI 分析/报告命令、旧生成器/存储读取器/知识编译器及其无用合同；比较模块也不再接受 legacy dossier。
- API 4318 与 4322 均使用主运行库，后台 worker 关闭；5177 与 5179 对应前端均可访问最新数据。
